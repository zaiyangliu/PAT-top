#include<cstdio>
int main(){
    int n;
    scanf("%d", &n);
    double temp;
    double sum;
    for(int i = 1; i <= n; ++i){
        scanf("%lf", &temp);
        sum += temp * i * (n-i+1);
    }
    printf("%.2f", sum);
    return 0;
}
